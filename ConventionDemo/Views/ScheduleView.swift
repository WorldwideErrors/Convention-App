import SwiftUI

struct ScheduleView: View {
    var body: some View {
        ZStack{
            Color("bg-color").ignoresSafeArea()
            Text("Schedule View")
                .font(.largeTitle)
            // Schedule View with button to details
        }
    }
}
